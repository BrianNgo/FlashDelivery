<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_footer_menu" >

    <TextView
        android:id="@+id/login_top_txt"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
 		android:gravity="center"
        android:textColor="@color/white"
        android:textSize="10sp"
        android:textStyle="bold" />
    
        
    <Button
        android:id="@+id/btnLogout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/login_top_txt"
        android:layout_alignBottom="@+id/login_top_txt"
        android:layout_alignParentRight="true"
        android:background="@drawable/normbox"
        android:minWidth="70dp"
        android:text="Logout"
        android:textColor="@color/white" />

</RelativeLayout>
