<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_footer_menu"
    android:orientation="horizontal"
    android:paddingTop="5dp" >

    <RelativeLayout
        android:layout_width="0dip"
        android:layout_height="wrap_content"
        android:layout_weight="1" >

        <ToggleButton
            android:id="@+id/tab_search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:drawableTop="@drawable/tab_search"
            android:onClick="changeTab"
            android:padding="0dp"
            android:tag="0"
            android:textColor="#FFcccccc"
            android:textOff="Search"
            android:textOn="Search"
            android:textSize="12sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <ToggleButton
        android:id="@+id/tab_orders"
        android:layout_width="0dip"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="@android:color/transparent"
        android:drawableTop="@drawable/tab_orders"
        android:onClick="changeTab"
        android:padding="0dp"
        android:tag="1"
        android:textColor="#FFcccccc"
        android:textOff="Orders"
        android:textOn="Orders"
        android:textSize="12sp"
        android:textStyle="bold" />

    <ToggleButton
        android:id="@+id/tab_account"
        android:layout_width="0dip"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="@android:color/transparent"
        android:drawableTop="@drawable/tab_account"
        android:onClick="changeTab"
        android:padding="0dp"
        android:tag="2"
        android:textColor="#FFFFFFFF"
        android:textOff="Account"
        android:textOn="Account"
        android:textSize="12sp"
        android:textStyle="bold" />

</LinearLayout>